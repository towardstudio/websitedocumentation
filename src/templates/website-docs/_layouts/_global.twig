{#
	Global Layout
	==============================
	Contains data and variables to be inherited by all layouts and templates.
#}

{# Set an object containing data inherited by all layouts and templates. #}
{% set global = {
	settings: websitedocumentation.getSettings(),
	url: websitedocumentation.getUrl(),
	logo: websitedocumentation.getSettings().logo[0] | default(null),
	colors: {
		brandBackground: websitedocumentation.getSettings().brandBgColor | default('ffffff'),
		brandText: websitedocumentation.getSettings().colors.brandText | default('000000'),
		accentBackground: websitedocumentation.getSettings().accentBgColor | default('000000'),
		accentText: websitedocumentation.getSettings().colors.accentText | default('ffffff')
	},
	bundles: {
		websiteDocumentation: 'bluegg\\websitedocumentation\\assetbundles\\DocumentationAsset',
		cmsGuide: 'bluegg\\websitedocumentation\\assetbundles\\CMSGuideAsset',
		styleGuide: 'bluegg\\websitedocumentation\\assetbundles\\StyleGuideAsset'
	}
} %}

{# All HTML output to be rendered. Should generally be left blank. #}
{% block html %}

{% endblock %}
